# The username who should own the process.
#
# ISPN_SERVER_USER=ispn

# The amount of time to wait for startup
#
# STARTUP_WAIT=30

# The amount of time to wait for shutdown
#
# SHUTDOWN_WAIT=30

# Location to keep the console log
#
# ISPN_SERVER_CONSOLE_LOG=/var/log/infinispan-server/console.log
# MODE=clustered, standalone

NAME='infinispan'
MODE='clustered'
BROADCAST_ADDR='37.6.2.63'

ISPN_SERVER_HOME=/local/apps/$NAME
ISPN_SERVER_LOG_HOME=/local/logs/$NAME
ISPN_SERVER_CONF_HOME=/local/conf/$NAME

ISPN_SERVER_CONSOLE_LOG=$ISPN_SERVER_LOG_HOME/console.log
ISPN_SERVER_CONFIG=$MODE.xml
ISPN_SERVER_RUN_CONF=$ISPN_SERVER_CONF_HOME/$MODE.conf
ISPN_SERVER_SCRIPT=$MODE.sh
